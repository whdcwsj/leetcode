#include<bits/stdc++.h>
using namespace std;


int main(){
    int t;
    cin>>t;
    vector<string>store;
    vector<int>nums;
    vector<int>long_store;
    for(int i=0;i<t;i++){
        int n,k;
        cin>>n>>k;
        long_store.push_back(n);
        nums.push_back(k);
        string temp;
        store.push_back(temp);
    }

    system("pause");
    return 0;
}